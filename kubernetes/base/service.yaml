apiVersion: v1
kind: Service
metadata:
  name: lti-example-tool
spec:
  selector:
    app.kubernetes.io/component: web
    app.kubernetes.io/name: lti-example-tool
  ports:
    - name: http
      port: 80
      targetPort: http
